version: '3.9'

services:
  cassandra:
    image: cassandra:4.1
    container_name: ecommerce_cassandra
    ports:
      - "9042:9042"
    environment:
      - CASSANDRA_CLUSTER_NAME=ecommerce_cluster
    volumes:
      - cassandra_data:/var/lib/cassandra

volumes:
  cassandra_data:
